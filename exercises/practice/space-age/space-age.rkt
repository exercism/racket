#lang racket

(provide on-mercury
         on-venus
         on-earth
         on-mars
         on-jupiter
         on-saturn
         on-uranus
         on-neptune)

(define (on-mercury seconds)
  (error "Please implement 'on-mercury'"))

(define (on-venus seconds)
    (error "Please implement 'on-venus'"))

(define (on-earth seconds)
  (error "Please implement 'on-earth'"))

(define (on-mars seconds)
    (error "Please implement 'on-mars'"))

(define (on-jupiter seconds)
    (error "Please implement 'on-jupiter'"))

(define (on-saturn seconds)
    (error "Please implement 'on-saturn'"))

(define (on-uranus seconds)
    (error "Please implement 'on-uranus'"))

(define (on-neptune seconds)
    (error "Please implement 'on-neptune'"))
