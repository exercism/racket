#lang racket

(provide make-robot
         name
         reset!
         reset-name-cache!)

(define (make-robot)
  (error "Not implemented yet"))

(define (name robot)
  (error "Not implemented yet"))

(define (reset! robot)
  (error "Not implemented yet"))

(define (reset-name-cache!)
  (error "Not implemented yet"))
